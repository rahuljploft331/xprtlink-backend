{
  "info": {
    "_postman_id": "c1f7b7cb-4b3d-4c3e-8848-12cd80a1c6a3",
    "name": "Catalog Service",
    "description": "REST APIs for app configuration, active category listing, and static CMS pages (e.g. privacy policies, terms, support).",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "App Config",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url}}/api/v1/catalog/app-config",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "v1",
            "catalog",
            "app-config"
          ]
        },
        "description": "Splash / Update validation: returns minimum app version, force update, and maintenance state."
      }
    },
    {
      "name": "Get Active Categories",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url}}/api/v1/catalog/categories",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "v1",
            "catalog",
            "categories"
          ]
        },
        "description": "Fetch active categories for filters, dashboard listing, and search options."
      }
    },
    {
      "name": "Get CMS Page by Slug",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url}}/api/v1/catalog/cms/:slug",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "v1",
            "catalog",
            "cms",
            ":slug"
          ],
          "variable": [
            {
              "key": "slug",
              "value": "privacy"
            }
          ]
        },
        "description": "Retrieve static page markdown content. Slugs include: 'privacy', 'terms', 'help'."
      }
    }
  ]
}
